interface:
  display_name: "Client Delivery"
  short_description: "Scope, verify, release, and document client work safely"
  brand_color: "#F04A23"
  default_prompt: "Use $client-delivery to orient, plan, verify, and deliver this client project change safely."
policy:
  allow_implicit_invocation: true
